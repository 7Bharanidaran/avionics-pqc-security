import { Server, Radio, Network } from 'lucide-react'
import { api } from '../services/api'
import { useApi } from '../hooks/useApi'
import { EmptyState, ErrorState, LoadingState, Panel, Status, shortId } from '../components/common'
const iconFor = (type: string) => type.includes('GROUND') ? Radio : type.includes('NAVIGATION') ? Network : Server
export function Overview() { const { data, loading, error } = useApi(api.avionics); return <><div className="page-heading"><h1>SYSTEM OVERVIEW</h1><p>Simulated avionics nodes and configured cryptographic controls.</p></div>{loading ? <LoadingState label="RETRIEVING SYSTEM INVENTORY…" /> : error ? <ErrorState message={error} /> : !data?.entities.length ? <EmptyState /> : <><Panel title="AVIONICS TOPOLOGY"><div className="topology">{data.entities.map((node, i) => { const Icon = iconFor(node.component_type); return <div className="topology-item" key={node.id}><div className="node"><Icon size={19} /><div><strong>{node.name.replace(/ \(.+\)/, '')}</strong><small>{node.id}</small></div><Status value={node.status} /></div>{i < data.entities.length - 1 && <div className="connector" aria-hidden="true" />}</div> })}</div></Panel><div className="two-col"><Panel title="NODE INVENTORY"><table><thead><tr><th>NODE ID</th><th>FUNCTION</th><th>STATUS</th><th>ED25519 FINGERPRINT</th></tr></thead><tbody>{data.entities.map(n => <tr key={n.id}><td>{n.id}</td><td>{n.role}</td><td><Status value={n.status} /></td><td className="mono">{shortId(n.public_keys.ed25519 ?? 'N/A')}</td></tr>)}</tbody></table></Panel><Panel title="CRYPTOGRAPHIC CONFIGURATION"><dl className="config"><dt>KEY EXCHANGE</dt><dd>X25519 + ML-KEM</dd><dt>AUTHENTICATION</dt><dd>Ed25519 + SLH-DSA</dd><dt>KEY DERIVATION</dt><dd>HKDF-SHA256</dd><dt>ENCRYPTION</dt><dd>AES-256-GCM</dd><dt>TRANSPORT</dt><dd>UNTRUSTED SIMULATED DATALINK</dd></dl></Panel></div></>}</> }
